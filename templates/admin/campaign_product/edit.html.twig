<!DOCTYPE html>

<title>Edit CampaignProduct</title>

{% block body %}
    <h1>Edit CampaignProduct</h1>

    {{ include('campaign_product/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_campaign_product_index') }}">back to list</a>

    {{ include('campaign_product/_delete_form.html.twig') }}
{% endblock %}
