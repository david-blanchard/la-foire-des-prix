<form method="post" action="{{ path('admin_campaign_product_delete', {'id': campaign_product.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ campaign_product.id) }}">
    <button class="btn">Delete</button>
</form>
