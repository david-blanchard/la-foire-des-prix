<!DOCTYPE html>

<title>New CampaignProduct</title>

{% block body %}
    <h1>Create new CampaignProduct</h1>

    {{ include('campaign_product/_form.html.twig') }}

    <a href="{{ path('app_campaign_product_index') }}">back to list</a>
{% endblock %}
